# Optional Environment Variables for Adobe Hackathon Dashboard

# Custom dataset file path (Default: adobe_hackathon_results.json in project root)
DATA_FILE_PATH=adobe_hackathon_results.json

# Server Port (automatically set by Render / Railway / Heroku)
PORT=8000
