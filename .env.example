# Adobe Hackathon 2026 Dashboard - Environment Configuration

# Custom dataset file path (Default: adobe_hackathon_results.json in project root)
# Set this variable if your dataset file is stored in a custom path or mounted volume.
DATA_FILE_PATH=adobe_hackathon_results.json

# Server Port (automatically configured on cloud platforms like Render / Railway / Vercel)
PORT=8000

